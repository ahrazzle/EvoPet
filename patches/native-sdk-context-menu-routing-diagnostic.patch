diff --git a/src/primitives/canvas/widget_layout.zig b/src/primitives/canvas/widget_layout.zig
index b520a23..21c85ef 100644
--- a/src/primitives/canvas/widget_layout.zig
+++ b/src/primitives/canvas/widget_layout.zig
@@ -307,6 +307,7 @@ fn layoutAnchoredChildren(
     for (children) |child| {
         const anchor = child.layout.anchor orelse continue;
         const child_frame = anchoredWidgetFrame(child, anchor, anchor_rect, output[0].frame, tokens);
+        layout_log.warn("DIAG anchored frame kind={any} x={d} y={d} w={d} h={d} anchor_x={d} anchor_y={d} anchor_w={d} anchor_h={d}", .{ child.kind, child_frame.x, child_frame.y, child_frame.width, child_frame.height, anchor_rect.x, anchor_rect.y, anchor_rect.width, anchor_rect.height });
         _ = try layoutWidgetDepth(child, child_frame, parent_index, depth + 1, output, len, tokens);
     }
 }
diff --git a/src/runtime/canvas_widget_context_menu.zig b/src/runtime/canvas_widget_context_menu.zig
index dc49c63..292948a 100644
--- a/src/runtime/canvas_widget_context_menu.zig
+++ b/src/runtime/canvas_widget_context_menu.zig
@@ -76,11 +76,16 @@ pub fn RuntimeCanvasWidgetContextMenu(comptime Runtime: type) type {
         /// press-and-hold (`on_hold`). Declared context menus always win
         /// over hold handlers.
         pub fn presentCanvasWidgetContextMenuFromPointer(self: *Runtime, app: runtime_api.App(Runtime), input_event: platform.GpuSurfaceInputEvent) anyerror!void {
-            const routed = CanvasWidgetEventMethods().routeCanvasWidgetPointerInput(self, input_event, &self.widget_event_route_entries) catch |err| switch (err) {
-                error.WindowNotFound, error.ViewNotFound, error.InvalidViewOptions => return,
-                else => return err,
+            context_menu_log.warn("DIAG context input window={d} view={s} kind={any} button={d} x={d} y={d}", .{ input_event.window_id, input_event.label, input_event.kind, input_event.button, input_event.x, input_event.y });
+            const routed = CanvasWidgetEventMethods().routeCanvasWidgetPointerInput(self, input_event, &self.widget_event_route_entries) catch |err| {
+                context_menu_log.warn("DIAG route error={s} entries=0 hit=0", .{@errorName(err)});
+                return switch (err) {
+                    error.WindowNotFound, error.ViewNotFound, error.InvalidViewOptions => {},
+                    else => return err,
+                };
             };
             const pointer_event = routed orelse return;
+            context_menu_log.warn("DIAG route ok entries={d} hit={d}", .{ pointer_event.route.len, if (pointer_event.target) |target| target.id else 0 });
             const index = runtimeFindViewIndex(self, input_event.window_id, input_event.label) orelse return;
             const point = geometry.PointF.init(input_event.x, input_event.y);
 
@@ -93,7 +98,13 @@ pub fn RuntimeCanvasWidgetContextMenu(comptime Runtime: type) type {
             // presents as an anchored canvas surface instead: the app
             // loop answers the request event by mounting it — one
             // authored menu, platform-appropriate presentation.
-            if (deepestContextMenuRouteNode(self, index, pointer_event.route)) |node_index| {
+            const menu_node = deepestContextMenuRouteNode(self, index, pointer_event.route);
+            context_menu_log.warn("DIAG menu node={d} depth={d} items={d}", .{
+                if (menu_node) |node_index| node_index else 0,
+                if (menu_node) |node_index| self.views[index].widget_layout_nodes[node_index].depth else 0,
+                if (menu_node) |node_index| self.views[index].widget_layout_nodes[node_index].widget.context_menu.len else 0,
+            });
+            if (menu_node) |node_index| {
                 const widget = self.views[index].widget_layout_nodes[node_index].widget;
                 const count = @min(widget.context_menu.len, items.len);
                 if (count == 0) return;
@@ -112,6 +123,7 @@ pub fn RuntimeCanvasWidgetContextMenu(comptime Runtime: type) type {
                         .kind = .app,
                     }, point, items[0..count])) return;
                 }
+                context_menu_log.warn("DIAG fallback request dispatched target={d}", .{widget.id});
                 try self.dispatchEvent(app, .{ .canvas_widget_context_menu_request = .{
                     .window_id = input_event.window_id,
                     .view_label = self.views[index].label,
diff --git a/src/runtime/ui_app.zig b/src/runtime/ui_app.zig
index bedf1c3..cacd22a 100644
--- a/src/runtime/ui_app.zig
+++ b/src/runtime/ui_app.zig
@@ -3591,6 +3591,7 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
         /// same declared items as an anchored canvas surface on the
         /// target.
         fn handleContextMenuRequest(self: *Self, runtime: *Runtime, request_event: core.CanvasWidgetContextMenuRequestEvent) anyerror!void {
+            std.log.scoped(.zero_context_menu).warn("DIAG fallback request received window={d} view={s} target={d}", .{ request_event.window_id, request_event.view_label, request_event.target_id });
             const label_len = @min(request_event.view_label.len, self.context_menu_fallback_label_storage.len);
             @memcpy(self.context_menu_fallback_label_storage[0..label_len], request_event.view_label[0..label_len]);
             self.context_menu_fallback_label_len = label_len;
